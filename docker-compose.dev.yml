version: "3.8"

services:
  aws-services-dev:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aws-services-api-dev
    restart: always
    ports:
      - "${PORT:-5001}:${PORT:-5001}"
    env_file:
      - .env
    environment:
      NODE_ENV: development
